'use strict';
var loaded=false;
function ensure(){if(!loaded){importScripts('https://cdn.jsdelivr.net/npm/pdf-lib@1.17.1/dist/pdf-lib.min.js');loaded=true;}}
function enhance(ctx,w,h,mode){if(mode==='none')return;var id=ctx.getImageData(0,0,w,h),d=id.data,min=255,max=0;for(var i=0;i<d.length;i+=4){var g=Math.round(.299*d[i]+.587*d[i+1]+.114*d[i+2]);d[i]=d[i+1]=d[i+2]=g;if(g<min)min=g;if(g>max)max=g;}var range=Math.max(1,max-min),factor=mode==='strong'?1.9:mode==='contrast'?1.8:mode==='light'?1.2:mode==='table'?1.6:1.5, bw=mode==='strong'||mode==='table';for(var j=0;j<d.length;j+=4){var v=Math.round((d[j]-min)*255/range);v=Math.max(0,Math.min(255,Math.round((v-128)*factor+128)));if(bw)v=v>140?255:0;d[j]=d[j+1]=d[j+2]=v;}ctx.putImageData(id,0,0);}
async function run(d){ensure();if(!Array.isArray(d.images)||!d.images.length)throw Error('No scan images supplied.');var doc=await PDFLib.PDFDocument.create(),quality=Math.max(.5,Math.min(.98,Number(d.quality)||.92));for(var i=0;i<d.images.length;i++){var item=d.images[i],blob=new Blob([item.buffer],{type:item.type||'image/jpeg'}),bmp=await createImageBitmap(blob,{imageOrientation:'from-image'}),W=bmp.width,H=bmp.height,maxPx=6000000,scale=Math.min(1,Math.sqrt(maxPx/(W*H))),cw=Math.max(1,Math.round(W*scale)),ch=Math.max(1,Math.round(H*scale)),canvas=new OffscreenCanvas(cw,ch),ctx=canvas.getContext('2d');ctx.fillStyle='#fff';ctx.fillRect(0,0,cw,ch);ctx.drawImage(bmp,0,0,cw,ch);bmp.close();enhance(ctx,cw,ch,d.enhancement||'auto');var jpg=await canvas.convertToBlob({type:'image/jpeg',quality:quality}),bytes=new Uint8Array(await jpg.arrayBuffer()),img=await doc.embedJpg(bytes),page=doc.addPage([img.width,img.height]);page.drawImage(img,{x:0,y:0,width:img.width,height:img.height});canvas.width=0;canvas.height=0;self.postMessage({type:'scan-progress',page:i+1,total:d.images.length,percent:Math.round(((i+1)/d.images.length)*95)});}var out=await doc.save({useObjectStreams:true});if(!out||out.length<500)throw Error('Generated scan PDF appears empty.');return out;}
self.onmessage=async e=>{var d=e.data||{};if(d.type!=='scan-to-pdf')return;try{var b=await run(d);self.postMessage({type:'scan-to-pdf-done',buffer:b},[b]);}catch(err){self.postMessage({type:'scan-to-pdf-error',message:err&&err.message||String(err)});}};
